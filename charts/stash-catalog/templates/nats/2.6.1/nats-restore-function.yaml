{{ if .Values.nats.enabled }}
apiVersion: stash.appscode.com/v1beta1
kind: Function
metadata:
  name: 'nats-restore-2.6.1'
  labels:
    {{- include "stash-catalog.labels" . | nindent 4 }}
spec:
  args:
  - restore-nats
  - --provider=${REPOSITORY_PROVIDER:=}
  - --bucket=${REPOSITORY_BUCKET:=}
  - --endpoint=${REPOSITORY_ENDPOINT:=}
  - --region=${REPOSITORY_REGION:=}
  - --path=${REPOSITORY_PREFIX:=}
  - --scratch-dir=/tmp
  - --enable-cache=${ENABLE_CACHE:=true}
  - --max-connections=${MAX_CONNECTIONS:=0}
  - --hostname=${HOSTNAME:=}
  - --source-hostname=${SOURCE_HOSTNAME:=}
  - --interim-data-dir=${INTERIM_DATA_DIR}
  - --nats-args=${args:=}
  - --streams=${streams:=}
  - --overwrite=${overwrite:=false}
  - --wait-timeout=${waitTimeout:={{ .Values.waitTimeout}}}
  - --namespace=${NAMESPACE:=default}
  - --appbinding=${TARGET_NAME:=}
  - --snapshot=${RESTORE_SNAPSHOTS:=}
  - --output-dir=${outputDir:=}
  - --license-apiservice=${LICENSE_APISERVICE:=}
  - --storage-secret-name=${storageSecretName:=}
  - --storage-secret-namespace=${storageSecretNamespace:=}
  - --warning-threshold=${warningThreshold:=30s}
  image: '{{ include "catalog.registry" . }}/stash-nats:2.6.1-v1'
{{ end }}
